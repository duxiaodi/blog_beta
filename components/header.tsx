import Nav from "./nav";

const Header = () => {
  return (
    <header className="absolute right-5 z-[9] flex items-center h-14 flex items-center">
      <Nav />
    </header>
  );
};

export default Header;
